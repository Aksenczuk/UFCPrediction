UFC Fight Prediction Software

To run the application follow these steps:

1. pip install the libraries in requirements.txt
2. open code editor of choice / cmd in root folder
3. run this command in the terminal " python -m streamlit run app.py "
